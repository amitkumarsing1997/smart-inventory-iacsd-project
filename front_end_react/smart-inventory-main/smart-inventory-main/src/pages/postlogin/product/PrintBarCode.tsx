import { Flex, Heading } from '@chakra-ui/react';
import React from 'react';

export const PrintBarCode = () => {
    return (
        <Flex direction={'column'}>
            <Heading fontSize={'xl'}>Print BarCode</Heading>
        </Flex>
    )
}